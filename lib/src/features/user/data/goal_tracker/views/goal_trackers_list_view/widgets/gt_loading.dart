import 'package:flutter/material.dart';

class GTLoading extends StatelessWidget {
  const GTLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
