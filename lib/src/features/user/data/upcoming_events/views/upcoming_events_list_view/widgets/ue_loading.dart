import 'package:flutter/material.dart';

class UELoading extends StatelessWidget {
  const UELoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
